{
	"name": "futureui",
	"version": "0.0.13",
	"author": {
		"name": "Dev By RayRay",
		"email": "raymon@byrayray.dev"
	},
	"description": "Future UI a Web Component Library for Vanilla JS and more",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/devbyray/future-ui.git"
	},
	"workspaces": [
		"packages/core-ui",
		"packages/theme-ui"
	],
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "lerna run build",
		"dev": "lerna run dev",
		"publish": "lerna run build && npm run core:publish && npm run theme:publish",
		"core:build": "lerna run build --scope @fui/core-ui",
		"core:dev": "lerna run dev --scope @fui/core-ui",
		"core:publish": "npm run core:build && cd packages/core-ui && npm publish",
		"theme:build": "lerna run build --scope @fui/theme-ui",
		"theme:dev": "lerna run dev --scope @fui/theme-ui",
		"theme:publish": "npm run theme:build && cd packages/theme-ui && npm publish"
	},
	"keywords": [],
	"license": "MIT",
	"dependencies": {},
	"devDependencies": {
		"lerna": "^8.0.1"
	}
}
